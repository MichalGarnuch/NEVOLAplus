@model NEVOLAplus.Data.Models.CMS.Page
@inject NEVOLAplus.Portal.Services.ITextSnippetService SnippetService

@{
    // Tytuł strony: "Delete Page"
    ViewData["Title"] = await SnippetService.GetContentByKeyAsync("Page_Delete_Title")
                       ?? "Delete";
}

<h1>@ViewData["Title"]</h1>

<h3>
    @(await SnippetService.GetContentByKeyAsync("Page_Delete_ConfirmText")
        ?? "Are you sure you want to delete this?")
</h3>
<div>
    <h4>@(await SnippetService.GetContentByKeyAsync("Page_EntityName_Label") ?? "Page")</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LinkTitle)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LinkTitle)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Heading)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Heading)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Content)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DisplayOrder)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DisplayOrder)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="PageId" />
        <input type="submit"
               value="@(await SnippetService.GetContentByKeyAsync("Page_Delete_Button")
                        ?? "Delete")"
               class="btn btn-danger" />
        |
        <a asp-action="Index">
            @(await SnippetService.GetContentByKeyAsync("Page_BackToList_Link")
                ?? "Back to List")
        </a>
    </form>
</div>
